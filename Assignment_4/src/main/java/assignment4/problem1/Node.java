package assignment4.problem1;

/**
 * Represents an interface with default method
 */
public interface Node {

  /**
   * Generate strings
   *
   * @param sb      StringBuilder
   * @param grammar Grammar
   */
  void generateString(StringBuilder sb, Grammar grammar);
}
